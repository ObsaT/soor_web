/* Client-side environment variables.
   Edit or replace this file per-deployment. */
window.__env = window.__env || {};
// API base URL - ensure trailing slash
window.__env.API_BASE_URL = window.__env.API_BASE_URL || 'http://127.0.0.1:5200/';
